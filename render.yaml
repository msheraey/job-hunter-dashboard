services:
  - type: web
    name: job-hunter-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_dashboard_simple:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
